# === Core Web Framework ===
# App runs on FastAPI + uvicorn (ASGI, async).
weasyprint
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
python-socketio[asyncio]>=5.11.0
starlette>=0.37.0
itsdangerous>=2.1.2

# === Azure SDKs ===
azure-identity==1.15.0
azure-mgmt-authorization==4.0.0
azure-mgmt-compute==30.0.0
azure-mgmt-consumption==10.0.0
azure-mgmt-costmanagement==4.0.1
azure-mgmt-monitor==4.0.0
azure-mgmt-network==25.0.0
azure-mgmt-recoveryservices==2.0.0
azure-mgmt-resource==25.0.0
azure-mgmt-resourcegraph
azure-mgmt-sql==3.0.1
azure-mgmt-storage==24.0.1
azure-mgmt-subscription==3.1.1
azure-mgmt-web==7.0.0
azure-monitor-query
# === Azure SDKs — Extended Services ===
azure-mgmt-containerservice>=26.0.0
azure-mgmt-containerinstance>=9.0.0
azure-mgmt-keyvault>=10.0.0
azure-mgmt-redis>=13.0.0
azure-mgmt-cosmosdb>=9.0.0
azure-mgmt-datafactory>=3.0.0
azure-mgmt-logic>=9.0.0
azure-mgmt-eventhub>=11.0.0
azure-mgmt-servicebus>=8.0.0
azure-mgmt-iothub>=3.0.0
azure-mgmt-cognitiveservices>=13.0.0
azure-mgmt-applicationinsights>=2.0.0
azure-mgmt-cdn>=12.0.0
azure-mgmt-apimanagement>=3.0.0

# === Database ===
# psycopg2-binary / asyncpg removed — app uses SQLite (Go engine: go-sqlite3, Python: aiosqlite).
# Add back if you adopt PostgreSQL as the primary store.
sqlalchemy==2.0.25
aiosqlite>=0.19.0  # Async SQLite driver

# === Data & ML ===
# scipy, pyarrow, stable-baselines3, gymnasium removed — zero usages in codebase.
numpy==1.26.3
pandas==2.2.0
scikit-learn==1.4.0
statsmodels==0.14.1

# === Security ===
cryptography==42.0.5

# === Utilities ===
boto3==1.34.0
python-dotenv==1.0.0
pyyaml==6.0.1
requests==2.31.0
httpx[http2]>=0.27.0
openai>=1.50.0
anthropic>=0.34.0
# google-generativeai is intentionally omitted — codebase uses google-genai (google.genai) exclusively
google-genai>=1.15.0

tenacity>=8.2.3
eval-type-backport>=0.2.0     # Python 3.9 typing compatibility for Pydantic
