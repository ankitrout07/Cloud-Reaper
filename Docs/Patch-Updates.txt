# Cloud-Reaper Remaining Patch Updates Plan

This plan describes the implementation details for completing the remaining patch updates from `Docs/Patch-Updates.txt` in the `Cloud-Reaper` workspace.

---

## Proposed Changes

We will systematically implement the requirements in the following components:

### 1. Database Layer

#### [MODIFY] [models.py](file:///Users/ankitanupamrout/git/Cloud-Reaper/src/reaper/engine/models.py)
We will add the new SQLAlchemy models: `Budget`, `BudgetAlert`, and `CloudCommitment`. Because `init_db()` is called automatically when Flask starts up, these tables will be generated seamlessly.

- **`Budget`**: Represents static or seasonally adjusted cost envelopes.
- **`BudgetAlert`**: Notification dispatch configurations linked to a specific budget.
- **`CloudCommitment`**: Multi-cloud reserved instance and savings plan contract tracker.

---

### 2. Flask Application Routing

#### [MODIFY] [app.py](file:///Users/ankitanupamrout/git/Cloud-Reaper/src/reaper/web/app.py)
We will add a new endpoint `/about` to serve our custom and premium About page.
```python
@app.route("/about")
def about():
    return render_template("about.html")
```

---

### 3. User Interface Templates & Views

#### [NEW] [about.html](file:///Users/ankitanupamrout/git/Cloud-Reaper/src/reaper/web/templates/about.html)
We will create a high-fidelity glassmorphic page displaying details about the Cloud-Reaper project, features, and a premium card highlighting the author with their LinkedIn profile (`https://www.linkedin.com/in/ankitrout07`). We'll style it to match the existing sci-fi/neon cybernetic aesthetics.

#### [MODIFY] [_sidebar.html](file:///Users/ankitanupamrout/git/Cloud-Reaper/src/reaper/web/templates/_sidebar.html)
- Add a new "About" link at the bottom.
- Inject a bell icon link for Alerts/Notifications between "Build with AI" and "Financial Intelligence" sections, specifically at line 32 above the "Financial Intelligence" header.

#### [MODIFY] [financial.html](file:///Users/ankitanupamrout/git/Cloud-Reaper/src/reaper/web/templates/financial.html)
- Enrich the "Budget Alerts" tab with structured and beautiful card modules for:
  - **Cost Alerts**: Notification threshold settings (Learn More).
  - **Report Notifications**: Scheduled report dispatches.
  - **All Costs Weekly**: Weekly summary dispatches.
  - **Integrations**: Discord, Slack, and Microsoft Teams broadcast hubs.
- Integrate Discord and Slack brand colors and logos directly into inputs, titles, and labels of the webhook settings card.

#### [MODIFY] [integrations.html](file:///Users/ankitanupamrout/git/Cloud-Reaper/src/reaper/web/templates/integrations.html)
- Add a new **Kubernetes Agent card** inside the "Static Connections" section.
- Incorporate a direct link to `docs.html#kubernetes` (Docs) and a premium toggle switch.
- Include a simulated toggle function `toggleK8sAgent(element)` showing toast feedback for activation status.

#### [MODIFY] [index.html](file:///Users/ankitanupamrout/git/Cloud-Reaper/src/reaper/web/templates/index.html)
- Create a dedicated, premium **System Overview & Architecture** card on the main dashboard (`index.html`).
- This section will display the copied topology image (`/static/img/Pasted_image.png`) with responsive dimensions, alongside high-fidelity metrics and descriptions of Cloud-Reaper's architecture.

---

### 4. Stylesheets & Static Assets

#### [MODIFY] [main.css](file:///Users/ankitanupamrout/git/Cloud-Reaper/src/reaper/web/static/css/main.css)
- Append a comprehensive set of Light Theme overrides under the `[data-theme="light"]` selector using the `!important` flag.
- The overrides will calibrate hardcoded background utilities (`bg-white/5`, `bg-black/40`, `glass-card`, `glass-morph`), text colors (`text-white`, `text-slate-300`), form elements (`glass-input`), and accents (`glow-text-cyan`, `text-cyan-400`, `text-green-400`, `text-blue-400`) to guarantee high contrast, solid legibility, and visual excellence under light mode.

#### [NEW] [favicon.png](file:///Users/ankitanupamrout/git/Cloud-Reaper/src/reaper/web/static/assets/favicon.png)
- Standardize and upgrade the brand favicon.ico / favicon.png using high-fidelity graphics.

---

### 5. Automated Tests

#### [MODIFY] [test_engine_models.py](file:///Users/ankitanupamrout/git/Cloud-Reaper/tests/unit/test_engine_models.py)
We will add new unit tests for `Budget`, `BudgetAlert`, and `CloudCommitment` to assert that they instantiate correctly with all required schemas.

---

## Verification Plan

### Automated Tests
- We will execute `pytest tests/unit/test_engine_models.py` to ensure SQLAlchemy models function properly.
- Run `pytest` across the entire workspace to check for regressions.

### Manual Verification
- Launch the Flask web server using `python main.py` or equivalent and navigate through:
  - **Main Dashboard**: Inspect the "System Overview & Architecture" card and check if `Pasted_image.png` renders perfectly.
  - **Sidebar**: Ensure the Bell Icon link and the "About" link are correctly positioned, styled, and navigable.
  - **About Page**: Ensure the glassmorphic LinkedIn card and social elements look stunning and match the cybernetic design system.
  - **Budget Alerts**: Toggle light/dark modes and verify text readability of Slack/Discord configurations and the newly enriched card modules.
  - **Integrations**: Check that the Kubernetes card renders with its toggle, and the simulated agent activator produces visual toast notifications.
